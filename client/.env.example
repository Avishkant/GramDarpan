## Client .env example for Vite (do NOT commit secrets here)
# Vite-only variables must start with VITE_
# If VITE_API_BASE is empty, client uses relative /api paths and relies on the dev-server proxy.
VITE_API_BASE=
VITE_BACKEND_URL=
VITE_CLIENT_URL=http://localhost:5173

